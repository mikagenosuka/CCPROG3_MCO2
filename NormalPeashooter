/** The subclass NormalPeashooter represents a plant
 * that can be planted by the player
  *
  * @author Darryl Ac-ac & Brian Garcia
  * @version 1.1
  */


public class NormalPeashooter extends Peashooter {

    /**
     * Constructs a Normal Peashooter at the given location.
     *
     * @param row the row where the plant is placed
     * @param col the column where the plant is placed
     */
    public NormalPeashooter(int row, int col) {
        super(row, col);
    }

    /**
     * Fires a normal pea into the game model.
     * This method is called from the superclass when the cooldown is over and a zombie is in range.
     *
     * @param model the current GameModel instance
     */
    @Override
    protected void fireProjectile(GameModel model) {
        model.addPea(new Pea(row, col + 1)); // starts one tile to the right of the shooter
        System.out.println("Normal Peashooter at (" + row + "," + col + ") fired a pea.");
    }

    /**
     * Returns the sun cost to place this peashooter.
     *
     * @return sun cost of 100
     */
    @Override
    public int getCost() {
        return 100;
    }
}
